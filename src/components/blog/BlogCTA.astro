---
export interface Props {
  variant?: 'default' | 'compact';
}

const { variant = 'default' } = Astro.props;
---

{variant === 'compact' ? (
  <!-- Compact CTA - dla środka artykułu -->
  <div class="not-prose my-8 p-6 bg-gradient-to-r from-[#2b9e40]/10 to-[#0b74b5]/10 dark:from-[#2b9e40]/20 dark:to-[#0b74b5]/20 rounded-xl border-2 border-[#2b9e40]/30 dark:border-[#2b9e40]/40">
    <div class="text-center">
      <p class="text-lg font-semibold text-gray-800 dark:text-gray-200 mb-3">
        Potrzebujesz pomocy w automatyzacji procesów?
      </p>
      <a
        href="/kontakt"
        class="inline-flex items-center justify-center px-6 py-3 text-base font-medium text-white bg-[#2b9e40] hover:bg-[#238a36] rounded-full transition-all duration-200 hover:shadow-lg hover:scale-105"
      >
        Skontaktuj się z nami
      </a>
    </div>
  </div>
) : (
  <!-- Full CTA - dla końca artykułu -->
  <div class="not-prose my-12 p-8 bg-gradient-to-br from-[#2b9e40] to-[#0b74b5] rounded-2xl shadow-xl">
    <div class="max-w-3xl mx-auto text-center text-white">
      <h3 class="text-2xl md:text-3xl font-bold mb-4">
        Gotowy na automatyzację swojego biznesu?
      </h3>
      <p class="text-lg mb-6 text-white/90">
        Skorzystaj z naszego doświadczenia w automatyzacji procesów biznesowych. Pomożemy Ci zaoszczędzić czas i zwiększyć efektywność.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/kontakt"
          class="inline-flex items-center justify-center px-8 py-4 text-base font-bold text-[#2b9e40] bg-white hover:bg-gray-50 rounded-full transition-all duration-200 hover:shadow-lg hover:scale-105"
        >
          Umów bezpłatną konsultację
        </a>
        <a
          href="/automatyzacja-procesow-biznesowych"
          class="inline-flex items-center justify-center px-8 py-4 text-base font-bold text-white bg-white/20 hover:bg-white/30 border-2 border-white rounded-full transition-all duration-200 hover:shadow-lg"
        >
          Zobacz nasze usługi
        </a>
      </div>
    </div>
  </div>
)}
